# Generated by Containerization Assist (AI)
apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic
  namespace: spr-pclinic-mig
  labels:
    app.kubernetes.io/name: spring-petclinic
    app.kubernetes.io/component: web
  annotations:
    # Expose publicly via an Azure Load Balancer.
    # Change to "ClusterIP" if you plan to use an Ingress controller instead.
    service.beta.kubernetes.io/azure-load-balancer-health-probe-request-path: /actuator/health
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: spring-petclinic
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
