# Generated by Containerization Assist (AI)
apiVersion: v1
kind: ConfigMap
metadata:
  name: spring-petclinic-config
  namespace: spr-pclinic-mig
  labels:
    app.kubernetes.io/name: spring-petclinic
    app.kubernetes.io/component: config
data:
  # Default profile uses H2 in-memory database — no external DB required.
  # To use MySQL, set SPRING_PROFILES_ACTIVE=mysql and supply MYSQL_URL, MYSQL_USER, MYSQL_PASS.
  # To use PostgreSQL, set SPRING_PROFILES_ACTIVE=postgres and supply POSTGRES_URL, POSTGRES_USER, POSTGRES_PASS.
  SPRING_PROFILES_ACTIVE: "default"
  SERVER_PORT: "8080"
  MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: "*"
  # Uncomment and populate for MySQL:
  # MYSQL_URL: "jdbc:mysql://<mysql-host>:3306/petclinic"
  # Uncomment and populate for PostgreSQL:
  # POSTGRES_URL: "jdbc:postgresql://<postgres-host>:5432/petclinic"
