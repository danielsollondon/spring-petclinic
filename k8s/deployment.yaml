# Generated by Containerization Assist (AI)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-petclinic
  namespace: spr-pclinic-mig
  labels:
    app.kubernetes.io/name: spring-petclinic
    app.kubernetes.io/component: web
    app.kubernetes.io/version: "4.0.0-SNAPSHOT"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: spring-petclinic
  template:
    metadata:
      labels:
        app.kubernetes.io/name: spring-petclinic
        app.kubernetes.io/component: web
        app.kubernetes.io/version: "4.0.0-SNAPSHOT"
    spec:
      # Never use the default service account in production
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: spring-petclinic
          # Replace <your-acr-name> with your Azure Container Registry name after pushing the image.
          # Example: myregistry.azurecr.io/spring-petclinic:v1
          image: <your-acr-name>.azurecr.io/spring-petclinic:v1
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: spring-petclinic-config
          # Sensitive DB credentials should be stored in a Kubernetes Secret or Azure Key Vault.
          # Uncomment the block below and create the secret before deploying with a database:
          # env:
          #   - name: MYSQL_USER
          #     valueFrom:
          #       secretKeyRef:
          #         name: spring-petclinic-db-secret
          #         key: username
          #   - name: MYSQL_PASS
          #     valueFrom:
          #       secretKeyRef:
          #         name: spring-petclinic-db-secret
          #         key: password
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          # Spring Boot Actuator liveness/readiness probes
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false   # Spring Boot writes temp files at runtime
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: spring-petclinic
